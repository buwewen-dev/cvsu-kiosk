# Local development uses SQLite if DATABASE_URL is not set.
# For production (Render), DATABASE_URL is set automatically from the linked database.

# DATABASE_URL=postgres://user:password@host:5432/dbname

# Port to listen on (Render sets this automatically)
# PORT=3000
